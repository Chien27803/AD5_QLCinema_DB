<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền dùng Internet: bắt buộc để upload Cloudinary -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Quyền đọc ảnh trong máy (Android 13+ dùng READ_MEDIA_IMAGES) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Cho các máy Android cũ hơn (<= 12) nếu cần đọc file ảnh từ storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        tools:replace="android:fullBackupContent"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AD5"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".UserListActivity" />
        <activity android:name=".MovieManagementActivity" />
        <activity android:name=".RoomManagementActivity" />
        <activity
            android:name=".StatisticsActivity"
            android:label="Thống kê doanh thu"
            android:exported="false" />

    </application>

</manifest>
